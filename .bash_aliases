# ls
alias lsa='ls -a'
alias lsl='ls -l'
alias lsal='ls -al'
alias lsla='ls -al'

# delete folder
alias del='rm -rfi'

# sudo apt install
alias sai='sudo apt install '

# sudo
alias suco='sudo'

# cd
alias cd..='cd ..'

# suckless
alias slinstall='sudo cp config.def.h config.h ; sudo make install'
